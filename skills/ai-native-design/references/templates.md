# Output templates (optional)

Use these when the user wants a specific structure or when you need a stable format for long answers.

## Default: concise suggestion list
- Findings:
- Proposed boundaries:
- Contracts:
- Policy/version:
- Tests:
- Migration steps:
- Risks/non-goals:

## One-line glossary (optional)
- Presentation = entrypoints/triggers
- Application/Facade = orchestration
- Domain = rules/invariants/decisions
- Infra/Adapters = DB/SDK/IO specifics

## Optional: expanded memo (only if user asks)
1) Goal & constraints
2) Current coupling map (include Mermaid)
3) Proposed architecture + boundaries (include Mermaid)
4) Public contracts (types/APIs/errors)
5) Policy/version plan (selection + persistence)
6) Test strategy (domain/integration/e2e)
7) Migration plan (phases + rollback)
8) Open questions / risks
